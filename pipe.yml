pipelines:
  - name: varsha_gitRepo
    steps:
      - name: input_git_bash
        type: Bash
        configuration:
          nodePool: varsha_u16
          runtime:            
            type: image
            image:
              #custom:
              #  name: drydock/u16java
               # tag: master
        execution:
          onExecute:
            - printenv
            - docker images
            - java -version
            - mvn --version
            - ant -version 
            - gradle --version 
